%\Load error if {scrlettr.cls} because {scrlettr is deprecated - use scrletter2}
\Load error if {epsf}          because {epsf          is deprecated—use graphicx}
\Load error if {psfig}         because {psfig         is deprecated—use graphicx}
\Load error if {doublespace}   because {doublespace   is deprecated—use setspace}
\Load error if {fancyheadings} because {fancyheadings is deprecated—use fancyhdr}
\Load error if {scrpage}       because {scrpage       is deprecated—use scrlaye—crpage}
\Load error if {scrpage2}      because {scrpage2      is deprecated—use scrlaye—crpage}
%\Load error if {caption}[<2008/08/24] because {use 2008.08.24 instead}
\Load error if {isolatin1}     because {isolatin      is deprecated—use [latin1]inputenc}
\Load error if {umlaut}        because {umlaut        is deprecated—use [latin1]inputenc}
\Load error if {t1enc}         because {t1enc         is deprecated—use [T1]fontenc}
%\Load error if {dinat.bst} because {dinat.bst is deprecated—use natdin.bst}
\Load error if {glossary}      because {glossary      is deprecated—use glossaries}
\Load error if {SIunits}       because {SIunits       is deprecated—use siunitx}
\Load error if {subfigure}     because {subfigure     is deprecated—use subcation}
\Load error if {subfig}        because {subfig        is deprecated—use subcaption}
\Load error if {csvtools}      because {csvtools      is deprecated—use datatool}
\Load error if {times}
\Load error if {mathptm}
\Load error if {pslatex}
\Load error if {palatino}
\Load error if {mathpple}
\Load error if {utopia}
\Load error if {euler}
\Load error if {ae}
\Load error if {zefonts}
