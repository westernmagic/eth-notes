\Load {keyval}   before {geometry} if {geometry}
\Load {ifpdf}    before {geometry} if {geometry}
\Load {ifvtex}   before {geometry} if {geometry}
\Load {ifxetex}  before {geometry} if {geometry}
%\Load {atbegshi} before {geometry} if {geometry} %check source for conditions

\Load {geometry} after {pkgloader-cls-pkg}
